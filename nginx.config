server {
    listen 80;
    listen 443 ssl;

    resolver 127.0.0.11 valid=5s;
    ssl_certificate     /etc/ssl/server.crt;
    ssl_certificate_key /etc/ssl/server.key;

    server_name ~^(alpha|beta|gamma).*\.local\.dev\.auth0\.com$;

    location / {
        proxy_pass http://$1;
    }
}
